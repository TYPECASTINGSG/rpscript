#!/usr/bin/env node
// const { exec } = require('child_process');
// exec('rps install basic ramda', (err, stdout, stderr) => {
//   if (err) {
//     console.log('Fail to install basic modules : ');
//     console.error(stderr);
//     return;
//   }
//   console.log(`stdout: ${stdout}`);
// });